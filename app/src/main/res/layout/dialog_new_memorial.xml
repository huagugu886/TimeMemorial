<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/miui_bg"
    android:paddingHorizontal="24dp"
    android:paddingTop="12dp"
    android:paddingBottom="40dp">

    <!-- Handle -->
    <View
        android:layout_width="36dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp"
        android:background="@drawable/bg_search_bar_handle" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/modal_new"
        android:textSize="20sp"
        android:textColor="@color/miui_text_primary"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/modal_name"
        android:textSize="13sp"
        android:textColor="@color/miui_text_secondary"
        android:layout_marginBottom="6dp" />

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="48dp"
        app:cardCornerRadius="14dp"
        app:cardBackgroundColor="@color/miui_search_bg"
        app:cardElevation="0dp"
        app:strokeWidth="0dp"
        android:layout_marginBottom="16dp">

        <EditText
            android:id="@+id/etName"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@null"
            android:hint="输入纪念日名称"
            android:textColorHint="@color/miui_text_tertiary"
            android:textColor="@color/miui_text_primary"
            android:textSize="15sp"
            android:singleLine="true"
            android:paddingHorizontal="16dp" />
    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/modal_date"
        android:textSize="13sp"
        android:textColor="@color/miui_text_secondary"
        android:layout_marginBottom="6dp" />

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="48dp"
        app:cardCornerRadius="14dp"
        app:cardBackgroundColor="@color/miui_search_bg"
        app:cardElevation="0dp"
        app:strokeWidth="0dp"
        android:layout_marginBottom="16dp">

        <EditText
            android:id="@+id/etDate"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@null"
            android:hint="选择日期"
            android:textColorHint="@color/miui_text_tertiary"
            android:textColor="@color/miui_text_primary"
            android:textSize="15sp"
            android:singleLine="true"
            android:focusable="false"
            android:clickable="true"
            android:paddingHorizontal="16dp" />
    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/modal_desc"
        android:textSize="13sp"
        android:textColor="@color/miui_text_secondary"
        android:layout_marginBottom="6dp" />

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="80dp"
        app:cardCornerRadius="14dp"
        app:cardBackgroundColor="@color/miui_search_bg"
        app:cardElevation="0dp"
        app:strokeWidth="0dp"
        android:layout_marginBottom="16dp">

        <EditText
            android:id="@+id/etDesc"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@null"
            android:hint="添加备注..."
            android:textColorHint="@color/miui_text_tertiary"
            android:textColor="@color/miui_text_primary"
            android:textSize="15sp"
            android:gravity="top"
            android:paddingHorizontal="16dp"
            android:paddingVertical="12dp"
            android:inputType="textMultiLine" />
    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/modal_category"
        android:textSize="13sp"
        android:textColor="@color/miui_text_secondary"
        android:layout_marginBottom="6dp" />

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:layout_marginBottom="24dp">

        <LinearLayout
            android:id="@+id/formCats"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal" />
    </HorizontalScrollView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCreate"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="@string/modal_create"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@android:color/white"
        app:backgroundTint="@color/miui_brand"
        app:cornerRadius="14dp" />
</LinearLayout>